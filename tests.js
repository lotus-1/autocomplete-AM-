var test = require('tape');
var testAnimal = require('/.word.txt');

test('Testing tape', function(t) {
  t.equal(1, 1, "tape is working");
  t.end();
});

test('To return a string', function(t) {
  var actual = 
  var expected =
})
